events {}

http {
    server {
        listen 8080;

        # 👉 Route all /api/* calls to FastAPI
        location /api/ {
            proxy_pass http://127.0.0.1:8000;
        }

        # 👉 Everything else goes to Next.js
        location / {
            proxy_pass http://127.0.0.1:3000;
        }
    }
}
